const BASEURL = "/";

export const APP_PATHS = {
  ROOT: BASEURL,

  /** TEMPLATE */
  TEMPLATE: `${BASEURL}template`,

  ANTD: `${BASEURL}antd`,

  SEGMENT: `${BASEURL}segment`,

  TAILWIND: `${BASEURL}tailwind`,
} as const;
